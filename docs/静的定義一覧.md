# 静的定義（static / const）一覧

コードベース内の静的定義を分類し、動的にすべき候補と対応状況をまとめました。

---

## 1. 拡張子・ファイル種別の静的定義

### 1.1 抽出器の対応拡張子（現状は静的のまま）

| 場所 | 内容 | 備考 |
|------|------|------|
| **TextFileExtractor.cs** | `SupportedExtensionSet`（.txt, .csv, .log, .md, .cs, ...） | 抽出器が対応する拡張子を静的定義。新規拡張子はコード変更が必要。動化は別タスク（低優先度）。 |
| **OfficeExtractor.cs** | `SupportedExtensionSet`（.docx, .xlsx, .pptx） | 同上。 |
| **PdfExtractor.cs** | `SupportedExtensionSet`（.pdf） | 同上。 |

### 1.2 プレビュー種別（対応済み：抽出器から導出）

プレビュー表示は **すべてテキスト（行リスト）** です。OfficeExtractor も PreviewCategory=Text とし、.docx/.xlsx/.pptx はテキスト抽出の行表示のみ。表形式・HTML プレビューは行いません。抽出器の `PreviewCategory` は GetExtractor で参照されるが、現状は表示経路の分岐には使わず、拡張子の二重管理はありません。

（過去のドキュメントにあった `PreviewHelper` の `OfficeExtensions` / `TextExtensions` / `ImageExtensions` / `GetImageMimeType` は廃止済み。現行の **`PreviewHelper`** は `NormalizeExtension` のみを提供し、拡張子正規化に使う。）

### 1.3 シンタックスハイライト用（現在は未使用）

かつては `PreviewHelper.LanguageMap` と `AppSettings.ExtensionLanguageMap` を用いた **シンタックスハイライト用の拡張子→言語マップ** を持っていましたが、  
プレビュー処理から Highlight.js ベースのシンタックスハイライト機能を削除したため、これらの定義は現在のコードベースからも削除されています。

---

## 2. パス・設定の静的定義

| 場所 | 内容 | 備考 |
|------|------|------|
| **FullTextSearch.Core / DefaultPaths.cs** | `IndexPath`（LocalApplicationData/FullTextSearch/Index） | **デフォルトパスはここに一元化済み**。AppSettings.IndexPath のデフォルト値および LuceneSearchService のフォールバックはいずれもこれを参照。 |
| **FullTextSearch.Core / IndexMessages.cs** | `SkippedLogHeaderLine` / `SkippedLogTotalLine` / `GetFileTypeDisplayName` | スキップログ先頭・合計行、Lucene `filetype` 用の拡張子→表示名（`LuceneIndexService`）。 |
| **AppSettingsService.cs** | `SettingsPath`（LocalApplicationData/FullTextSearch/settings.json） | 設定ファイルのパス。環境に依存するが定数として妥当。 |
| **AppSettingsService.cs** | `JsonOptions` | シリアライズオプション。変更頻度低い。 |

---

## 3. Lucene・検索まわりの定数（変更すると互換性に影響）

| 場所 | シンボル | 値 | 備考 |
|------|----------|-----|------|
| **LuceneIndexService.cs** | FieldFilePath | "filepath" | インデックスフィールド名。変更すると既存インデックスと互換が崩れる。 |
| **LuceneIndexService.cs** | FieldFileName | "filename" | 同上。 |
| **LuceneIndexService.cs** | FieldFolderPath | "folderpath" | 同上。 |
| **LuceneIndexService.cs** | FieldContent | "content" | 同上。 |
| **LuceneIndexService.cs** | FieldFileSize, FieldLastModified, FieldFileType, FieldIndexedAt | "filesize", "lastmodified", "filetype", "indexedat" | 同上。 |
| **LuceneIndexService.cs** | AppLuceneVersion | LUCENE_48 | Lucene バージョン。 |
| **LuceneIndexService.cs** | ParallelExtractCount | 48 | 並列抽出数。 |
| **LuceneSearchService.cs** | AppLuceneVersion | LUCENE_48 | 同上。 |
| **LuceneSearchService.cs** | HighlightFragmentSize | 100 | ハイライト抜粋のフラグメント長。 |
| **LuceneSearchService.cs** | MaxHighlights | 5 | 最大ハイライト数。 |
| **LuceneSearchService.cs** | ReaderOpenRetryMs | 150 | リーダーオープンリトライ間隔（ミリ秒）。 |
| **LuceneSearchService.cs** | FilenameBoost | 2.5f | ファイル名マッチのブースト係数。 |
| **LuceneSearchService.cs** | MaxQueryTerms | 64 | クエリ最大トークン数。 |
| **LuceneSearchService.cs** | MaxQueryClauses | 256 | クエリ最大句数。 |
| **ContentLimits.cs** | LuceneMaxTermUtf8Bytes | 32765 | Lucene の 1 トークンあたり最大 UTF-8 バイト数。 |
| **ContentLimits.cs** | IndexMaxContentChars | 100_000 | 1 ドキュメントあたりインデックス格納最大文字数。 |
| **ContentLimits.cs** | IndexMaxFileBytesForExtract | 10MB | このサイズを超えるファイルは本文抽出しない（プレビュー・テキスト読込上限と同じ）。 |
| **ContentLimits.cs** | ExtractMaxChars | 100_000 | 抽出器が返す最大文字数。 |
| **ContentLimits.cs** | PreviewMaxFileBytes | 10MB | プレビューを試みる最大ファイルサイズ。 |
| **ContentLimits.cs** | MaxTextFileBytesToRead | 10MB | テキストファイルとして読み込む最大サイズ。 |
| **SudachiTokenizer.cs** | MaxTermUtf8Bytes | 32765 | Lucene トークン長制限（同上）。 |
| **SudachiTokenizer.cs** | MaxInputCharsForTokenize | 500_000 | トークナイズ入力の最大文字数。 |
| **SudachiTokenizer.cs** | MaxBatchDocuments | 40 | バッチあたり最大ドキュメント数。 |
| **SudachiTokenizer.cs** | MaxCharsPerContentInBatch | 80_000 | バッチ内 1 コンテンツあたり最大文字数。 |
| **SudachiTokenizer.cs** | StreamDelim | "---SUDACHI_DOC_END---" | Python とのプロトコル区切り。 |
| **SudachiTokenizer.cs** | StreamTimeoutMs | 60_000 | InvokeSudachiStream のウォッチドッグタイムアウト（ミリ秒）。 |
| **SudachiTokenizer.cs** | BatchTimeoutMs | 120_000 | InvokeSudachiBatch のウォッチドッグタイムアウト（ミリ秒）。 |
| **SudachiTokenizer.cs** | OneshotTimeoutMs | 60_000 | InvokeSudachiOneshot のウォッチドッグタイムアウト（ミリ秒）。 |
| **SudachiAnalyzer.cs** | MatchVersion | LUCENE_48 | Lucene バージョン。 |

これらは仕様・互換性のための定数として静的で問題なし。

---

## 4. プレビュー・UI まわりの定数

| 場所 | シンボル | 値 | 備考 |
|------|----------|-----|------|
| **App.xaml.cs** | Window.Title | `UserMessages.AppTitle`（"全文検索システム"） | ウィンドウタイトルバー・Alt+Tab。MAUI Windows では ContentPage.Title は反映されないためここで設定。 |
| **MainPage.xaml.cs** | ContentPage.Title | 同上（コンストラクタで設定） | XAML の `Title` は未使用。 |
| **FileSearch.Blazor.csproj** | ApplicationTitle | 全文検索システム（`UserMessages.AppTitle` と揃える） | スプラッシュ画面・パッケージ表示等。 |
| **ContentLimits.cs** | PreviewMaxChars | 50_000 | プレビュー表示の最大文字数。Core で一元定義。 |
| **ContentLimits.cs** | PreviewMaxLinesForHighlight | 50_000 | ハイライトをかける最大行数（Core）。 |
| **ContentLimits.cs** | PreviewLineHeightPx | 20 | プレビュー行の高さ（px）。Virtualize の ItemSize と揃える。 |
| **PreviewService.cs** | PreviewMaxChars | 50_000 | プレビュー上限（抽出テキストを切り詰める上限）。 |
| **Home.razor.cs** | PreviewDebounceMs | 200 | ファイル選択変更後、プレビュー読み込みを発行するまでのデバウンス（ミリ秒）。 |
| **Home.razor.cs** | ProgressReportInterval | 500 | 進捗表示の報告間隔。 |
| **Home.razor.cs** | ProgressReportThrottleMs | 250 | 進捗表示のスロットル（ミリ秒）。 |
| **Home.razor.cs** | MsgNoTargetFolders, MsgUpdateFailed, MsgRebuildFailed | （本文「7. エラー・メッセージ」参照） | インデックス操作時のユーザー向けメッセージ。 |
**index.html** | scrollToNextHighlight / scrollToPrevHighlight | （JavaScript関数） | ハイライトナビゲーション。**行単位で移動**（WinMerge風）。同じ行内の複数のmark要素をグループ化し、行インデックスで移動する。 |

※ プレビュー遅延（デバウンス）は上記 Home.razor.cs の `PreviewDebounceMs` のみ。AppSettings の `PreviewDelayMs` は削除済み。
※ ハイライトナビゲーションは行単位で動作する。同じ行に複数のmark要素があっても、その行は1つの単位として扱われ、次へ/前へで行単位で移動する。

---

## 5. ロック・キャッシュ用（問題なし）

| 場所 | 内容 | 備考 |
|------|------|------|
| **SudachiTokenizer.cs** | `SharedProcessLock`, `ScriptPathLock`, `PythonCacheLock` | マルチスレッド用ロック。静的でよい。 |
| **SudachiTokenizer.cs** | `StreamDelim` ("---SUDACHI_DOC_END---") | Python とのプロトコル。変更すると互換が崩れる。 |

---

## 6. エラー・メッセージ定義

ユーザー向け文言の**完全な一覧（エラー／警告／情報の分類・ID）**は **[メッセージ一覧.md](メッセージ一覧.md)** に集約した。多言語化や文言変更時はそちらを主に参照する。

本節はコード上の定数・主な出現箇所のメモとして残す。

### 6.1 インデックス・検索（`FileSearch.Messages/UserMessages.cs` + `Home` partial：`Home.razor.cs` / `Home.Search.Tree.cs` / `Home.Index.cs` など）

| シンボル / 出現箇所 | 文言 | 用途 |
|---------------------|------|------|
| `UserMessages.NoTargetFolders` | 対象フォルダを設定してください。 | 対象フォルダ未設定でインデックス操作したとき |
| `UserMessages.UpdateFailed` | 差分更新に失敗しました。 | 差分更新の例外時（続けて ex.Message を付与する場合あり） |
| `UserMessages.RebuildFailed` | インデックス構築に失敗しました。 | 再構築の例外時 |
| `UserMessages.SearchFailed` | 検索に失敗しました。 | 検索実行の catch 時 |
| `UserMessages.FormatSkippedCountLine` | `{N} 件がスキップされました` | インデックス完了後、スキップがある場合にフッターに表示。クリックで `skipped_files.log` を既定アプリで開く |

### 6.1.1 サイドバー（`SearchSidebar.razor` → `UserMessages`）

| シンボル | 用途 |
|----------|------|
| `SidebarTitleSearchResults` / `SearchInputPlaceholder` / `CannotSearchWhileIndexing` / `Searching` | ヘッダー・検索欄・インデックス中バナー・ツリー「検索中」 |
| `TreeEmptyKeywordPrompt` / `TreeEmptyNoResults` / `TreeEmptyPressEnterToSearch` | ツリー空の案内 |
| `FormatBuildingPercent` / `IndexBuildCancelTitle` / `Cancel` | 構築中フッター |
| `FormatRegisteredCount` / `LastUpdateSuffix` / `RebuildButtonTitle` / `Rebuild` | アイドル時フッター |

### 6.1.2 アプリ名・空状態・相対時刻・進捗（`UserMessages` + `DisplayFormatters`）

| シンボル / 場所 | 用途 |
|-----------------|------|
| `UserMessages.AppTitle` | ウィンドウ・ヘッダー・`index.html` title と一致させる |
| `EmptyMainSelectFileTitle` / `EmptyMainSelectFileHint` | `Home.razor` メイン未選択時 |
| `SkippedLogOpenTooltip(logFileName)` | `SearchSidebar` スキップ行の `title` |
| `LastIndexJustNow`, `FormatMinutesAgo` / `FormatHoursAgo` / `FormatDaysAgo` | `DisplayFormatters.FormatLastIndexUpdate` の相対表示（`LastIndexNeverRun` は未実行） |
| `FormatIndexProgressCounts` | `Home.Index.cs` インデックス進捗（スキップ付き） |
| `FormatHighlightNavWithLine` / `FormatHighlightNavCountsOnly` | `Home.Preview.cs` ハイライト位置表示 |
| `WebViewLoadError` / `WebViewReload` | `index.html` のエラー帯（HTML は静的のためコメントで対応関係を記載） |

### 6.1.3 設定モーダル・インデックス更新ダイアログ・プレビュー／フォルダ一覧（`UserMessages`）

| 出現箇所 | 主なシンボル |
|----------|----------------|
| **SettingsModal.razor** | `SettingsTitle`, `SettingsSectionDisplay`, `ThemeDark` / `ThemeLight` / `ThemeSystem`, `SettingsSectionTargetFolders`, `Add`, `Browse`, `SettingsPlaceholderFolderPath`, `SettingsSectionExtensions`, `SettingsNoteAfterExtensionChange`, `AriaRemove`, `SettingsSectionIndexPath`, `OpenIndexFolder`, `OpenIndexFolderTitle`, `SettingsSectionIndex`, `LabelLastIndexUpdate`, `LastIndexNeverRun`, `LabelAutoRebuild`, `AutoRebuildOff`〜`AutoRebuild1w`, `Save`, `Cancel` |
| **IndexUpdateDialog.razor** | `IndexUpdateDialogTitle`, `CloseDialog`（閉じるボタンの aria-label）, `IndexUpdateDialogDescription`, `IndexUpdateDiffTitle` / `IndexUpdateDiffDescription`, `IndexUpdateFullRebuildTitle` / `IndexUpdateFullRebuildDescription`, `Cancel`, `Execute` |
| **FilePreviewView.razor** | `FormatLineCount`, `PreviewGoPrevTitle` / `PreviewGoNextTitle`, `Prev` / `Next`, `PreviewNavPositionTitle`, `OpenFile` / `OpenFileTitle`, `OpenFolderShort` / `OpenFolderTitle`, `PreviewLoading` |
| **FolderListView.razor** | `FormatChildCount`, `FolderListGoParentTitle` / `FolderListGoParent`, `ColumnName`, `ColumnDate`, `ColumnExtension`, `ColumnSize`, `FilterTitle`, `FilterAll`, `KindFolder` |

### 6.2 プレビュー（PreviewService.cs, Home.razor.cs）

| 出現箇所 | 文言（`UserMessages`） | 用途 |
|----------|------------------------|------|
| PreviewService | `PreviewPathRequired` | プレビュー呼び出しでパスが空のとき |
| PreviewService | `PreviewNotAvailable` / `PreviewCancelledBracket` / `PreviewErrorLine(…)` / `PreviewTruncatedEllipsis` | 抽出器なし・キャンセル・抽出失敗・長文省略 |
| `Home.Preview.cs` | `PreviewLoadCancelled` / `PreviewErrorLine(…)` | プレビュー読み込みキャンセル・例外 |

### 6.3 設定画面・バリデーション（Home.razor.cs → SettingsEditState）

| プロパティ | 文言（`UserMessages`） | 用途 |
|------------|------|------|
| FolderMessage | `FolderPathRequired` | フォルダ追加で空のとき |
| FolderMessage | `FolderNotFound` | パスが存在しないとき |
| FolderMessage | `AlreadyAdded` | 同一フォルダを追加しようとしたとき |
| FolderMessage | `FolderPickerFailed(detail)` | 参照ダイアログ失敗時 |
| ExtensionMessage | `AlreadyAdded` | 同一拡張子を追加しようとしたとき |

### 6.4 例外・ログ（仕様把握用、ユーザーにそのままは出さない）

| 場所 | 内容 | 備考 |
|------|------|------|
| LuceneIndexService.cs | InvalidOperationException("IndexService has not been initialized. Call InitializeAsync first.") | 未初期化で検索・更新したとき |
| LuceneIndexService.cs | WriteSkippedLog → skipped_files.log | スキップファイルをインデックスフォルダ内にログ出力（日時・件数・パス一覧） |
| OfficeExtractor / PdfExtractor / TextFileExtractor | FileNotFoundException("File not found", filePath) | ファイルが存在しないとき |
| TextFileExtractor.cs | InvalidOperationException("File is too large: {size} bytes") | テキストファイルが MaxTextFileBytesToRead を超えたとき |

---

## 7. まとめ：動的化の対応状況

| 優先度 | 内容 | 状況 |
|--------|------|------|
| **高** | プレビュー種別を抽出器から導出 | **対応済み**。GetExtractor / PreviewCategory で導出。 |
| **中** | デフォルトインデックスパスの一元化 | **対応済み**。DefaultPaths.IndexPath に統一。 |
| **高** | LanguageMap / CodeExtensions の設定化 | **削除済み**。シンタックスハイライト機能廃止に伴い、拡張子→言語マップは現在使用していない。 |
| **低** | 抽出器の SupportedExtensionSet の動的化 | **別タスク**。設定やプラグインで拡張子を追加する設計は変更が大きいため未対応。 |
