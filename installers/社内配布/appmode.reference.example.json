{
  // ============================================================
  // 利用者 PC 用 appmode.json の例
  // 配置場所: Panoleon.exe と同じフォルダ
  // ファイル名: appmode.json（この example はコピーしてリネーム）
  // ============================================================

  // 参照専用モード（検索・プレビューのみ。設定変更・再構築は不可）
  "mode": "reference",

  // 管理者 PC と同じ shared.json を指す
  "sharedConfig": "\\\\fileserver\\panoleon\\shared.json"
}
