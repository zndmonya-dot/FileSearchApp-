# Panoleon 仕様変更まとめ（2026年5月19日〜6月18日）

5月19日時点の安定版（直前は **2026-04-26** `70385f8` 付近）から、本日（6月18日）までに入った主な仕様・挙動の変更をまとめる。

> **補足:** Git 上のコミットは **6月7日** から始まっている（5/19〜6/6 の履歴はなし）。

---

## 時系列（主要マイルストーン）

| 日付 | 概要 |
|------|------|
| 6/07 | 検索3モード（AND / OR / 完全一致）＋完全一致のバイグラム高速化 |
| 6/12 | 共有インデックス参照モード、非管理者 UI、並列度調整、自動更新の JST 境界 |
| 6/13 | 設定 UI 刷新、検索モード切替 UX |
| 6/14 | フォルダ閲覧・プレビュー・ローディングの大規模 UI 改善 |
| 6/15 | Panoleon ブランド統一、.msg 対応、Sudachi 辞書 ZIP 同梱 |
| 6/16 | Core 最適化、appmode.json 60 秒同期 |
| 6/17 | shared.json 連動、MSG 修正、サイズ上限設定 |
| 6/18 | 起動画面・検索進捗・列リサイズ、タイトル `Panoleon v2.0`、デッドコード整理 |

---

## 1. 検索機能

### 1.1 検索モード 3 種（6/07）

| モード | UI 表示 | 挙動 |
|--------|---------|------|
| AND検索 | デフォルト | スペース区切りの各語を**すべて**部分一致（同一行またはファイル名） |
| OR検索 | 新規 | スペース区切りの**いずれか**を部分一致 |
| 完全一致検索 | 新規 | 入力文字列が**連続してそのまま**含まれる（本文またはファイル名） |

- 完全一致は文字バイグラム索引で候補絞り込み → 連続一致で確定（日本語の部分文字列も取りこぼしにくい）
- 旧インデックス（バイグラム未収録）は全件走査へフォールバック
- プレビューのハイライトもモードに追従

### 1.2 検索 UI・進捗（6/12〜18）

- **検索ボタン**追加（Enter でも検索可）
- **モード切替時**: キーワードがあれば自動再検索、スピナー表示、再検索中はモード切替を無効化
- **検索件数上限を撤廃**（6/17）
- **検索進捗**（6/18）: 検証済みヒット総数を先出し、読み込みは `（n/N 件）` 形式。サイドバー見出し横の件数バッジは廃止

---

## 2. 共有インデックス・設定

### 2.1 `appmode.json`（実行ファイル横）

| キー | 説明 |
|------|------|
| `indexPath` | 共有インデックスのパス |
| `targetFolders` | クライアント初回起動時に検索フォルダへ反映（任意） |
| `forceNonAdmin` | UI 確認用。本番は省略または `false` |

- **起動時に読み込み**（`Initialize` / `ApplySharedSettingsFromAppMode`）。`shared.json` の変更追従は**アプリ再起動**（6/17 以降、60 秒ポーリングは廃止）

### 2.2 管理者判定

- **ローカルアカウント**（ユーザーのドメイン名＝PC 名）→ 管理者
- **ドメインアカウント**（AD）→ 非管理者（参照専用）

### 2.3 非管理者の権限

| 項目 | 可否 |
|------|------|
| 検索・プレビュー | 可 |
| インデックス再構築・自動更新 | 不可 |
| 設定: フォルダ・インデックスパス・自動更新 | 閲覧のみ（非活性） |
| 設定: テーマ・対象拡張子 | 個人設定として保存可 |

- 検索結果は個人の `TargetExtensions` で post-filter（共有インデックス本体は変更しない）
- 非管理者は `InitializeAsync(readOnly: true)` … IndexWriter を開かない

### 2.4 `shared.json`（6/17〜18）

- インデックスフォルダへ **自動書き込み**、起動時に反映
- 母体設定（`sharedConfig/shared.json`）との連動を整理

### 2.5 設定 UI（6/13〜18）

- GitHub 風のフォルダ指定 UI（パス手入力廃止、**フォルダを設定** のみ）
- 参照専用バッジ、非活性時の `cursor: not-allowed`
- インデックス保存先は **1 フォルダのみ**指定

---

## 3. インデックス

### 3.1 自動更新（6/12）

| 設定 | 挙動 |
|------|------|
| 毎日指定時刻 | チェックした時刻（JST）を過ぎ、当該枠で未更新なら差分更新 |
| 管理者 PC 非検索時 | 利用者の検索には影響しない |

- 検索操作から 30 秒以内は実行を見送り（従来どおり）

### 3.2 性能・安全性（6/12〜14）

- 他アプリ共存向けに並列度を抑制（パイプライン ≈ 4、Sudachi プール最大 2 等）
- **差分更新の誤削除防止**を強化
- 共有インデックス **初回参照**の不具合修正
- インデックス構築の **ETA（残り時間）** 表示

### 3.3 ファイルサイズ上限（6/17〜18）

- `shared.json` の例に `indexMaxFileBytes` キーを載せられるが、**現行の `AppModeService` は未読み込み**（JSON 例・将来用）
- **実行時**の本文抽出・インデックス・プレビュー上限は `ContentLimits.IndexMaxFileBytesForExtract`（**1MB 固定**）

---

## 4. ファイル形式・抽出

| 項目 | 変更内容 |
|------|----------|
| **Panoleon ブランド** | アプリ名・表示を Panoleon に統一（6/15） |
| **Outlook .msg** | MsgReader + CodePages 登録でインデックス・プレビュー対応 |
| **Sudachi 辞書** | `system_core.dic.zip` 同梱、ビルド時展開（GitHub 100MB 制限対応） |
| **対象拡張子** | 26 種に整理 |

---

## 5. UI / UX

### 5.1 フォルダ閲覧・一覧（6/14〜18）

- 起動時にフォルダツリーを一括構築し、**検索前から**ファイル選択・プレビュー可能
- フォルダ一覧に **「内容」列**（先頭行 / 検索時はマッチ行）を追加
- 列幅ドラッグを **エクスプローラー式** に変更（6/18）
- `C:\` 配下の登録件数表示・列リサイズの誤作動を修正

### 5.2 プレビュー（6/14）

- 統一ツールバー（一致移動・開く・フォルダ）
- パンくずナビ、WinMerge 風描画（`preview.js`）
- 単一読み込み表示、ハイライトスクロール改善

### 5.3 ローディング・起動（6/14〜18）

- GitHub 風ドットアニメ（文言なしスピナー）
- インデックス再構築中は右メイン領域をロック
- **起動スプラッシュ**（6/18）: ファビコン＋アニメ、最低 3 秒表示、テーマ連動（settings / localStorage）

### 5.4 ウィンドウ（6/14〜18）

- 起動サイズ: Win11 メモ帳相当（作業領域の約 2/3 幅）
- タイトル: **`Panoleon v2.0`**（読み仮名表記は廃止）
- タイトルバー左アイコン: 試行後、非表示のため関連コード削除（6/18）

---

## 6. アーキテクチャ・保守

- UI ロジックを **Core** へ移動（`TreeBuilder`, `FolderTableColumnLayout`, `LoadingEta` 等）
- プレビュー取得を `ContentPreviewHelper` に統合
- 未使用 API・モデルフィールド・CSS・文言定数の **デッドコード削除**（複数回実施）
- 未使用 NuGet 依存の削除

---

## 7. コミット対応表（抜粋）

| 日付 | コミット | 概要 |
|------|----------|------|
| 2026-06-07 | `8533300` | AND/OR/完全一致検索 |
| 2026-06-12 | `1a18b9c` | 共有インデックス参照モード |
| 2026-06-12 | `c06ecf1` | インデックス並列度調整 |
| 2026-06-12 | `3ddee1b` | 非管理者 UI・拡張子個人保存 |
| 2026-06-12 | `34be965` | 初回参照修正・自動更新改善 |
| 2026-06-13 | `8f673dd` | モード切替時スピナー |
| 2026-06-13 | `609ba60` | 設定ダイアログ UI 刷新 |
| 2026-06-14 | `c8c3866` | 検索前フォルダ閲覧 |
| 2026-06-15 | `1fa3358` | Panoleon 統一、.msg、Sudachi ZIP |
| 2026-06-16 | `1aa1b1d` | appmode 60 秒同期 |
| 2026-06-17 | `7d4fdd0` | shared.json、MSG 修正、サイズ上限 |
| 2026-06-18 | `43f5663` | shared.json 自動出力、一覧不具合修正 |
| 2026-06-18 | `1cc9f61` | 起動画面・検索進捗・列リサイズ |
| 2026-06-18 | `8452deb` | タイトルバーアイコン削除、デッドコード整理 |

---

## 関連ドキュメント

- 詳細な設定手順: [`docs/appmode設定.md`](docs/appmode設定.md)
- 5月下旬以降の変更（一部重複）: [`docs/仕様変更リスト.md`](docs/仕様変更リスト.md)
